# Core audio/data stack
librosa
soundfile
audioread
numpy==1.26.4
pandas
scipy
protobuf==4.25.8
resampy
torchvggish
tqdm
matplotlib
plotly
scikit-learn
Pillow

# Model stack
transformers
accelerate
einops
timm
omegaconf
hydra-core
wandb
gdown

# Evaluation
# ViSQOL is installed by setup_instance.sh from google/visqol because it needs
# Bazel/native build dependencies before pip install.
# git+https://github.com/ashvala/AQUA-tk.git
