# Instance setup commands

apt update
apt install -y software-properties-common
add-apt-repository ppa:deadsnakes/ppa -y
apt update

cd /workspace

git clone https://github.com/Justien2005/ssl-diffusion-audio-inpainting.git
cd ssl-diffusion-audio-inpainting

git checkout new-v1

# Buat environment, install dependency, clone external repo jika belum ada,
# download CQT-Diff+ weights, dan download AudioMAE checkpoint dari Google Drive.
bash setup_instance.sh

# Aktifkan environment untuk setiap sesi terminal baru.
source env_instance.sh

# Smoke test dulu:
# - code_it_v2.py harus disetel DATASET_FRACTION = 0.01
# - NUM_EPOCHS = 1
# - n_eval_samples = 2
python code_it_v2.py

# Kalau smoke test sudah lolos, jalankan final pipeline.
source env_instance.sh
python code_final_run_v2.py
all --user --name=venv
---

all --user --name=venv
---

all --user --name=venv
---

python code_final_run_v2.py --phase train --models clap_maid --auto-stop
